Mobile-friendly pack
- ankita-portfolio-preview-mobile.html → standalone preview (inline data)
- index.html + data.json → ready for GitHub Pages
- robots.txt → indexable by search engines

What's new:
- Collapsible mobile menu (hamburger), large tap targets.
- Trimmed spacing on small screens; cards stack 1-per-row.
- Gmail compose opens with your address prefilled.
- Work vs Academic projects split.

To publish: upload index.html, data.json, robots.txt to your repo root.
